import { AddScoresPipe } from './add-scores.pipe';

describe('AddScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new AddScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
